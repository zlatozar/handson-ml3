An Agent is an active, autonomous participant:

- It can use tools (read files, search, execute, etc.).
- It can reason in multiple steps, chain actions, and make decisions.
- It has its own system prompt/instructions that shape its behavior.

Best for: doing work — generating code, analyzing a codebase, running workflows.